// This file was auto-generated by SourceMod (v1.11.0.6906)
// ConVars for plugin "8player-abm\SaferoomNaps.smx"


// Set to 1 if you want to input your own saferoom hp value. Uses the z_survivor_respawn_health value otherwise. Disabled by default.
// -
// Default: "0"
SafeRoomNaps_CanWeUseCustomValue "0"

// Not used unless hUseCustomValue is enabled. If your health is lower than the set value, it will be modified to that value when the next map loads. 
// -
// Default: "50"
SafeRoomNaps_CustomSaferoomHealth "55"

// Whether to remove black-and-white from players. Set to 0 to disable this feature. 
// -
// Default: "1.0"
SafeRoomNaps_RemoveBlackAndWhite "1.0"

// Multiplier of buffer health (from incap revive, pain pills, adrenaline) to keep for low health players. Default is 0.0, which means temp health is not kept at all. A value of 1 means all buffer health is kept. 
// -
// Default: "0.0"
SafeRoomNaps_TempHealthMultiplier "0.0"

// Multiplier of buffer health to keep for high health players who have not been incapped.(those above 50 real hp or the set saferoom health value). Default is 1.0, which means all their temp health is kept.
// -
// Default: "1.0"
SafeRoomNaps_VeryHealthyTempHealthMultiplier "0.0"


